apiVersion: v1
kind: Service
metadata:
  name: vllm-cpu-service
  labels:
    app: vllm-cpu
spec:
  selector:
    app: vllm-cpu
  ports:
  - port: 8000
    targetPort: 8000
    name: http
  type: ClusterIP
